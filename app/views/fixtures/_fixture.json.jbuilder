json.extract! fixture, :id, :fixture_api_id, :referee, :timezone, :date, :timestamp, :periods_first, :periods_second, :venue_id, :venue_name, :venue_city, :status_long, :status_short, :status_elapsed, :league_api_id, :teams_home_api_id, :created_at, :updated_at
json.url fixture_url(fixture, format: :json)
